#include <stdio.h> 
    int main()
    {
        int i = -1;
        if (i > 0) {
            printf("Hello, world!");
        } else {
            printf("HaHa");
        }
        return 0; 
    }

// 메인이라 불리우는 블록 안에 입력

//블록 1